# 安全策略

## 报告漏洞

**请不要用公开 Issue 报告安全漏洞。**

首选方式：在仓库的 **Security** 标签页点击 **Report a vulnerability**，通过 GitHub 的私密漏洞报告提交。这样只有维护者能看到内容。

如果该入口不可用，可以开一个**不含任何细节**的 Issue，只说"有一个安全问题，需要私密沟通渠道"，维护者会主动联系你。

请在报告中尽量包含：

- 受影响的版本 / commit
- 复现步骤或 PoC
- 影响范围（越权？信息泄露？RCE？）
- 你建议的修复方向（如有）

**响应预期**：个人项目，不承诺 SLA。会在看到后尽快确认，能修就修；无法修复或不在范围内的会明确说明。

## 支持范围

只有 `main` 分支的最新代码在维护范围内。自行修改过的分支、旧版本、以及你自己 fork 后改出来的问题，不在范围内。

## 部署者须知（重要）

这是一个**自托管**应用，安全性很大一部分取决于部署方式。以下几条是实际会出事的地方：

### 1. 必须改掉默认管理员密码

默认文件管理员是 `admin` / `admin123`（见 `application.yml` 的 `ADMIN_USER` / `ADMIN_PASS`）。
**公开部署前必须通过环境变量覆盖**：

```bash
export ADMIN_USER=yourname
export ADMIN_PASS='一个足够长的随机密码'
```

首次启动时如果没设置，程序会用默认值生成 `travel-auth.json` 并在日志里提示——**这条提示不是可选项**。

同理，示例账号 `demo@travel.cn / Demo@2026` 只用于本地演示，**生产环境请删除或改密**。

### 2. 不要提交运行时凭据与数据

`.gitignore` 已排除以下文件，**不要用 `git add -f` 强行提交**：

| 文件 | 内容 |
|---|---|
| `travel-auth.json` | 管理员 PBKDF2 口令哈希与盐 |
| `travel-secret.key` | 会话签名密钥（泄露即可伪造登录态） |
| `.env` | 数据库口令、`AI_API_KEY` 等 |
| `traveldb.mv.db` | 全部行程、账单、同行人信息 |
| `photos/` | 上传的照片原图 |

如果不慎推上去了：**仅仅删除文件不够**，历史里还在。需要用 `git filter-repo` 重写历史，并**轮换所有已泄露的凭据**（数据库口令、AI Key、管理员密码）。

### 3. 放在 HTTPS 后面

会话 Cookie `wbtrav_session` 由应用下发（`HttpOnly` + `SameSite=Lax`）。应用本身监听明文 HTTP，请在前面放 Nginx/Caddy 做 TLS 终止，否则登录态在公网上是明文传输的。

参考 `deploy/` 下的 Nginx 配置与 [DEPLOY.md](DEPLOY.md)。

### 4. AI 功能会外发数据

配置 `AI_API_KEY` 后，行程规划会把**你输入的目的地、天数、预算与偏好**发送给第三方模型服务（默认智谱 GLM）。照片/视觉相关能力会外发图片。

- 不配置 `AI_API_KEY`，这部分功能就是关闭的
- 不要把这些接口暴露给你不信任的人
- 涉及他人隐私的照片，慎用视觉能力

### 5. 反向代理与访问控制

应用内置的权限模型是「团队 / 活动 / 角色」（`OWNER > CO_OWNER > EDITOR > MEMBER`）加全局权限位。**它的定位是协作隔离，不是对抗恶意攻击者的安全边界。**

如果你要放到公网，请在反向代理层再加一道访问控制（Basic Auth、IP 白名单、VPN 等），不要只依赖应用内登录。

## 已知的非目标

以下明确**不在**安全承诺范围内，请不要作为漏洞报告：

- 首次启动时的默认凭据（这是刻意的可用性取舍，文档已反复说明要改）
- 未启用 TLS 时的中间人（部署者责任，见上文第 3 条）
- 单实例内存态限流在重启后清零（设计如此）
- 缺少多租户强隔离（本项目是个人/小团队自托管工具，不是 SaaS 平台）
